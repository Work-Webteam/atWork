{#
/**
 * @file
 * Theme override to display a region.
 *
 * !! Copied from region.html.twig
 *
 * Available variables:
 * - content: The content for this region, typically blocks.
 * - attributes: HTML attributes for the region div.
 * - region: The name of the region variable as defined in the theme's
 *   .info.yml file.
 * - group_theme: Set in the atwork_groups module, this is a value
 *   that the user chose to change the apperance of their group.
 * @ingroup templates
 *
 * @see template_preprocess_region()
 */
#}
{%
  set classes = [
  'section',
  'section-' ~ section|clean_class,
  group_theme,
]
%}

{% if content %}
  <div{{ attributes.addClass(classes) }}>
    {{ content }}
  </div>
{% endif %}
